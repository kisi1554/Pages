# にっぽん一周すごろく

日本地図の上を旅しながら、**おかね**・**ひみつのカード100まい**・**もくてきち**の
3つを追いかける学習すごろくゲーム。小学校低学年（ひらがな中心）向け。

`index.html` をブラウザで開くだけで遊べる（ビルドもサーバーも不要）。

- 遊ぶ人数: 1〜4人（プレイヤー＋CPU、合計2〜4人）
- 1プレイの目安: 20〜30分
- 保存: ブラウザの localStorage に自動セーブ（「つづきから」で再開）

---

## 3つの目的

| 目的 | 集めかた | 点数 |
|---|---|---|
| 💰 おかね | クイズの正解・おつかい・おみせの収益 | 20円 = 1点 |
| 🃏 ひみつのカード | 止まった県で1枚見つかる（全100枚） | 1枚 = 15点 |
| 📍 もくてきち | 1年ごとに変わる。一番のりで到着 | 1回 = 200点 |
| 🏪 おみせ | 各県で購入。毎年収益が入る | 1軒 = 15点 |

3年（＝24ターン）遊んで、合計点の一番高い人が優勝。

## 遊びかた

1. サイコロを振り、出た数だけ**1マスずつ隣の県へ**進む。緑に光った県をタップして選ぶ
   （直前にいた県へすぐ戻ることはできない。行き止まりだけは例外）。
2. 止まった県で、順番に次のことが起きる。
   1. **ごとうち情報** — 県名・県庁所在地・その県の豆知識
   2. **ひみつのカード** — その県のまだ持っていないカードを1枚（全部集めていればお金）
   3. **もくてきち判定** — 一番のりなら 800円＋でんせつカード。2番目以降も 250円（年1回まで）
   4. **おつかい判定** — 届け先に着いていれば報酬
   5. **ごとうちクイズ** — 3択。正解で 150円
   6. **アクション** — おみせを買う／おつかいを受ける（最大2件）
   7. **きせつの できごと** — 28%の確率。冬の雪国では1回休みなど
3. 2ターンで季節が1つ進む（はる→なつ→あき→ふゆ）。8ターンで1年。
4. 年の終わりに、持っているおみせの収益がまとめて入る。

点線（‥‥）はフェリー・橋・飛行機で渡る道。✔ が付いた県は、カードを全部集め終えた県。

## カードずかん

集めたカードは**ゲームをまたいで**ずかんに残る（`nippon-sugoroku-dex-v1`）。
1プレイで集まるのは20〜25枚程度なので、100枚コンプリートは長期の目標。

カードは 47都道府県 × 2枚 ＋ でんせつカード6枚 ＝ 100枚。
でんせつカード（ふじさん・しんかんせん・じょうもんすぎ など）は、
もくてきちに一番のりで到着したときだけ手に入る。

## ファイル構成

| ファイル | 中身 |
|---|---|
| `index.html` | 画面構造 |
| `style.css` | デザイン |
| `data-prefs.js` | 47都道府県のデータ（隣接・豆知識・クイズ・おみせ・地図の位置） |
| `data-cards.js` | ひみつのカード100枚 |
| `game.js` | ゲームロジック |

### 地図について

都道府県は実際の県庁所在地の緯度経度から配置している。ただし京都と滋賀のように
近すぎて丸が重なる県は、東西南北の関係を変えない範囲で `data-prefs.js` の
`MAP_ADJUST` で表示位置だけずらしている。沖縄県は地図に収めるため九州の南西に寄せた。

隣接関係（`adj`）は陸続きの県に加えて、フェリー・橋・飛行機で行き来する
7つのルート（`SEA_LINKS`）を含む。これで47都道府県が1つにつながっている。

## 調整したいとき

`game.js` の先頭にまとまっている。

```js
const TURNS_PER_YEAR = 8;    // 1年のターン数
const TOTAL_YEARS    = 3;    // 何年で終わるか
const START_MONEY    = 1000;
const QUIZ_REWARD    = 150;  // クイズ正解の報酬
const DEST_REWARD    = 800;  // もくてきち一番のりの報酬
const SHOP_RETURN    = 0.42; // おみせの年間利回り
const SCORE = { yenPerPoint: 20, perCard: 15, perArrival: 200, perShop: 15 };
const CPU_WANDER = 0.25;     // CPUが最短ルートを選ばない確率（強さの調整）
```

## プロトタイプとして未実装のもの

- 効果音・BGM（他アプリの `audio.js` 相当）
- プレイヤー同士のやりとり（他人のおみせに止まったときの支払いなど）
- 都道府県ごとの複数クイズ（いまは1県1問なので、同じ県に何度も止まると同じ問題が出る）
- カードの並べ替え・絞り込み（ずかんは地方順の固定表示）
